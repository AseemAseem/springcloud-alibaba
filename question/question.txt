--------
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
dialect的配置不能用MYSQL5InnoDBDialect，这个已经注释掉了。用对应版本的MySQL8Dialect之类就行，
因为innodb是默认引擎
--------
ScenarioG
又发现一个坑，视频中所用的Spring-Cloud-Alibaba版本，nacos配置中心那一块，bootstrap.yml
中的shared-dataids配置默认是找DEFAULT_GROUP分组下面的配置，
无法实现指定其他group的共享配置文件，翻了下源代码它是把组名写死了：
loadNacosDataIfPresent(compositePropertySource, dataId,
DEFAULT_GROUP, fileExtension, isRefreshable); ，真坑啊！
导致我一直公共配置文件不生效。后来网上查了一下，果然在2.2.0版本当中，
这一块就改成一个Config列表了，可以为里面每一个dataId指定group了

--------
网关自定义断言不生效咋回事，代码和例子里一样，配置也没错

2020-08-21 17:391回复

ScenarioG擦！这里有个神坑啊，原来Path=/xxx/** 里这个xxx不能和微服务名一样，否则不管是内置断言还
是自定义断言都不生效。
--------
哪位大佬能告知启动MQ时一直报错  nohup ./bin/mqnamesrv &

2020-07-18 19:54回复

我帅我先看定位到rocketMQ安装目录，打开conf/broker.conf   在最后添加
brokerIP1=192.168.1.3
namesrvAddr=192.168.1.3:9876
你把我的公网ip替换为你的公网ip就可以了
--------
网关那少了个pom文件，放在我这里的question文件夹里面。
